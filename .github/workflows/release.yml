name: Release APK

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  release:
    if: github.repository == 'dasoops/legado-with-MD3' && github.ref == 'refs/heads/main'
    uses: ./.github/workflows/auto-release.yml
    with:
      publish: true
    secrets: inherit
